---
# file: roles/ansible_container/tests/test.yml

- name: Exæmple playbook for using role coglinev3.ansible_container
  hosts: localhost
  remote_user: root
  roles:
    - { role: coglinev3.ansible_container }
  vars:
    ansible_container_become: true
