---
# file: roles/ansible_container/vars/RedHat.yml

# requirements for EPEL (RHEL/AlmaLinux/Amazon) systems
ansible_container_requirements:
  - initscripts
  - sudo
  - which
  - openssh-server
  - python3
  - python3-pip
  - python3-setuptools
