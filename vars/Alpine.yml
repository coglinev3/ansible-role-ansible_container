---
# file: roles/ansible_container/vars/Alpine.yml

# requirements for Debian OS family
ansible_container_requirements:
  - bash
  - ca-certificates
  - openrc
  - openssh
  - openssl
  - sudo
  - py3-pip
  - py3-yaml

ansible_container_build_dependencies:
  - python3-dev
  - libffi-dev
  - openssl-dev
  - build-base
